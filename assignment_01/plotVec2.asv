function plotVec2(matrix)

% check matrix height=2
if size(matrix, 2) ~=2
    warning('matrix height is not 2!');
    return;
end

figure();
hold on;
for colI=1:size(matrix, 1)
    plotMat = zeros(2)
    for rowI=1:size(matrix, 2)
        
    end % rowI
end % colI
xlim([-1, 1]);
ylim([-1, 1]);
xlabel('x');
ylabel('y');
